import CustomFieldManager from "@/components/CustomFieldManager";

export const metadata = { title: "Custom Fields – RIO Task" };

export default function CustomFieldsPage() {
  return <CustomFieldManager />;
}
